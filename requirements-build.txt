# Инструменты сборки, которые попадают в готовый exe и потому фиксируются
# наравне с зависимостями приложения: загрузчик PyInstaller вшивается в
# бинарник, так что его версия — часть поставки, а не среды разработки.
#
# Линтер и pytest сюда не входят: они на содержимое сборки не влияют.
pyinstaller==6.22.0
